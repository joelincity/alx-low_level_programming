#inlude "main.h"
/**
 * _islower - check if a char is lowercase
 * @c: is the char to be checked
 * Return: 1 if char is lowwer, otherwise 0.
 */

int _islower(int c);
{
	return (c >= 'a' && c <= 'z');
}
