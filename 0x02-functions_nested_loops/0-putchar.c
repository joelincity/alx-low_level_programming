#include "main.h"

/**
 * main - print _putchar
 * is written by bwave
 * Return: on success 1.
 * on error, -1 is reyurned, and errno is set appropriately.
 */

int main(void)
{
return (write(1, &c, 1));
}
