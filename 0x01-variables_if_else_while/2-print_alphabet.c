#include <stdio.h>
/**
 * main - And when I can't fall asleep, I play what I call the alphabet game
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		     (c <= 'z') {
			       putchar(c);
			       c++,
			}
	putchar('\n');
	return (0);
}
